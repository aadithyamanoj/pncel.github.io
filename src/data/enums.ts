export enum TagType {
  other,
  award,
  "open-source",
  venue,
  tapeout,
}

export enum NewsType {
  other,
}

export enum MemberRole {
  other = "Other",
  pi = "Principle Investigator",
  phd = "Ph.D.",
  ms = "Master",
  ug = "Undergrad",
  postdoc = "Postdoc",
  staff = "Staff",
  visitor = "Visitor",
  alumni = "Alumni",
}

export enum LinkIcon {
  default,
  pdf,
  video,
  github,
  website,
  arxiv,
}
